# 首次真实模型传输预检

2026-09-06 北京时间 10:52，使用官方 deepseek-v4-flash 完成两次合成请求。原生工具调用、完整消息回传及最终 MemCore JSON 解析均通过。

| 请求 | 输入 tokens | 输出 tokens | 缓存命中 tokens | 完整请求延迟 |
| --- | ---: | ---: | ---: | ---: |
| 请求 pilot_echo | 1159 | 42 | 0 | 734 ms |
| 回传工具结果后生成 JSON | 952 | 59 | 0 | 922 ms |
| 合计 | 2111 | 101 | 0 | — |

费用按当天公开空闲时段费率估算为 **0.003621 元**；共享阶段账本按更保守的峰值费率计入 **0.007242 元**，剩余可用预算 **49.992758 元**，未决预留为零。供应商账单金额未核对，不能把估算称为已确认扣款。[官方定价与时段规则](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)。

模型返回名为 deepseek-v4-flash；请求显式关闭思考、temperature=0、top_p=1、max_tokens=2048、非流式。第一条结果的原生 tool call 包含 index、id、type、function；第二条请求保留该消息及参数字符串。最终 speech 等于合成工具返回值。结果没有提供可验证的不可变服务版本，不用文档中的版本名冒充供应商返回字段。

此次使用源码与合成 JSON 的冻结副本执行，并绑定同一阶段账本身份。每次派发前预留最坏费用，成功用量经过严格校验再结算；没有重试。保留完整本地报告，后续收费步骤必须继续使用该阶段账本。

本次只有一个合成工具，且强制指定调用。它验证官方连接、原生工具协议、费用记录与最终 JSON，**不验证 MemCore 记忆正确率、自主工具选择、卡片策略效果、Akane 完整宿主或缓存收益**。两次延迟只是连接记录，不能作性能结论；源码冻结也尚未锁定整个依赖环境。

下一步需要把同一 transport/账本接入场景执行器，验证生产 embedding，复用关闭 care 的 Akane 提示，再进行四条场景运行及答案暴露审计。现有场景 manifest 继续保持 not_run。
